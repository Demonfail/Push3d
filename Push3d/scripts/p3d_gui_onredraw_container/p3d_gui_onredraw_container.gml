/// @func p3d_gui_onredraw_container(container, event)
/// @desc Defines how container handles a "redraw" event.
/// @param {real} container The container.
/// @param {real} event     The event to handle.
gml_pragma("forceinline");
argument0[? "redraw"] = true;